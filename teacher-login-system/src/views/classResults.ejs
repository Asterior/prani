<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/tailwind.css" rel="stylesheet">
    <title>Class Results</title>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-5">
        <h1 class="text-2xl font-bold mb-4">Results for Class: <%= className %></h1>
        
        <table class="min-w-full bg-white border border-gray-300">
            <thead>
                <tr>
                    <th class="py-2 px-4 border-b">Student Name</th>
                    <th class="py-2 px-4 border-b">Subject</th>
                    <th class="py-2 px-4 border-b">Score</th>
                </tr>
            </thead>
            <tbody>
                <% results.forEach(result => { %>
                    <tr>
                        <td class="py-2 px-4 border-b"><%= result.studentName %></td>
                        <td class="py-2 px-4 border-b"><%= result.subject %></td>
                        <td class="py-2 px-4 border-b"><%= result.score %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>

        <div class="mt-4">
            <h2 class="text-xl font-semibold">Overall Average: <%= overallAverage %></h2>
            <h2 class="text-xl font-semibold">Class Pass Percentage: <%= passPercentage %>%</h2>
        </div>
    </div>
</body>
</html>